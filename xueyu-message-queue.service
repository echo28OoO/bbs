[Unit]
Description=xueyu message queue
After=network.target

[Service]
User=root
WorkingDirectory=/var/www/xueyu
ExecStart=/usr/local/bin/celery -A tasks worker --loglevel=info
Restart=always

[Install]
WantedBy=multi-user.target